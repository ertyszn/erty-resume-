export const metadata = {
  title: "Edeh Linus Daniel",
  description: "Web3 Community Manager Portfolio"
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
